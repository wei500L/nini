[project]
name = "newsroom-backend"
version = "0.1.0"
description = "AI simulated interview practice backend"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.116,<1.0",
    "httpx>=0.28,<1.0",
    "sqlmodel>=0.0.24,<1.0",
]
